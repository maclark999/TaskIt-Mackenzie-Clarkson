<%= form_for(@project, html: {class: "form-horizontal"}) do |f| %>

<% if @project.errors.any? %>
  <div class = "col-md-offset-2">
  <div id = "error-explanation", class= "alert alert-danger">
    <h4><%= pluralize(@project.errors.count, "error") %> prohibited this form from being saved: </h4>
    <ul>
      <% @project.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  </div>
<% end %>

<div class="form-group">
  <label class="control-label col-md-2"><%= f.label :name %></label>
  <div class="form-controls col-md-4">
    <%= f.text_field :name, class: "form-control" %>
  </div>
</div>


<div class="form-group">
  <div class="actions">
    <div class="col-md-offset-2 col-md-4">
      <input class="btn btn-primary"<%= f.submit %>
      <%= link_to 'Cancel', projects_path, class: "btn btn-default" %>
    </div>
  </div>
</div>

<% end %>
